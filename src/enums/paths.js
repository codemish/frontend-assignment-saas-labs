import { ProjectDetails } from "../screens";

const PATHS = {
  PROJECT_DETAILS: {
    Component: ProjectDetails,
    name: "Project Details",
    url: "/product/details",
  },
};

export default PATHS;
